id: csv_schema
title: Validate inventory CSV schema
start_url: http://127.0.0.1:8765/download_center/index.html
goal: Download inventory.csv and validate required columns.
allowed_domains: ["127.0.0.1", "localhost"]
max_steps: 6
fixtures:
  static_port: 8765
validators:
  - type: downloaded_csv_columns
    params:
      columns: ["sku", "name", "quantity", "warehouse"]
tags: ["local", "download", "schema"]

